import { defineComponent, ref } from 'vue'

export default defineComponent({
    name: 'Palette gallery',
    setup() {    
        return () => (
        <div class="p-8">
            <p>Palette Gallery</p>
        </div>
        )
  }
})