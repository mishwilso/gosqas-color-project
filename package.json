{
  "name": "gosqas-color-project",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "azurite": "azurite-table --location ./azurite-data --tablePort 10002",
    "start": "npm run start --workspace=packages/backend",
    "dev": "npm run dev --workspace=packages/frontend",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces --if-present run --coverage"
  },
  "devDependencies": {
    "azurite": "^3.29.0",
    "concurrently": "^8.2.2"
  }
}
