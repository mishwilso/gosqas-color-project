{
  "files": [],
  "extends": "./.nuxt/tsconfig.json",
  "compilerOptions": {
    "baseUrl": ".", // The root of your project
    "paths": {
      "@/*": ["app/*"],
      "@components/*": ["app/components/*"],
      "@pages/*": ["app/pages/*"],
      "@assets/*": ["app/assets/*"]
    },
    // Other options typically configured in a Vue project:
    "target": "ESNext",
    "module": "ESNext",
    "moduleResolution": "bundler", // or "Node" if you are not using a modern bundler
    "allowJs": true, // Often needed for path resolution in non-TS files like .vue
    "strict": true,
    "jsx": "preserve",
    "lib": ["esnext", "dom"]
  },
  "include": ["app/**/*.ts", "app/**/*.tsx", "app/**/*.vue"]
}